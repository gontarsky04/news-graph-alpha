# Copy to .env and fill in before: docker compose up --build

# Required — OpenRouter API key (used by the extractor service for LLM extraction)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional
LLM_MODEL=anthropic/claude-haiku-4.5
NEO4J_PASSWORD=newsgraph
EXTRACTOR_DEADLINE_SECONDS=600
